package org.firstinspires.ftc.teamcode.Swerve.Enums;

/**
 * This enum is used to tell the wheel which direction it should be spinning
 */
public enum WheelDirection {
    FORWARD, BACKWARD, STATIC
}
